{
  "name": "socialsnag",
  "version": "1.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node build.js",
    "build:zip": "node build.js --zip",
    "test": "vitest run",
    "test:watch": "vitest",
    "lint": "eslint src/"
  },
  "devDependencies": {
    "@eslint/js": "^9.0.0",
    "esbuild": "^0.24.0",
    "eslint": "^9.0.0",
    "playwright": "^1.58.2",
    "vitest": "^3.0.0"
  }
}
